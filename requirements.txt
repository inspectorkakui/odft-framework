# Core dependencies
torch>=2.0.0
transformers>=4.30.0
datasets>=2.12.0
trl>=0.7.1
numpy>=1.24.0
pandas>=2.0.0
pyyaml>=6.0
tqdm>=4.65.0
matplotlib>=3.7.0
tensorboard>=2.13.0

# Optional dependencies (uncomment as needed)
# unsloth @ git+https://github.com/unslothai/unsloth.git  # For faster fine-tuning
# wandb>=0.15.0  # For experiment tracking
# scikit-learn>=1.2.0  # For metrics and evaluation
# ta-lib  # For technical analysis (trading example)
# bitsandbytes>=0.39.0  # For quantization
# pyarrow>=12.0.0  # For parquet file handling (trading example)